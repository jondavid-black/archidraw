# archidraw
A flexible drawing tool supporting everything from whiteboard to structured architectural languages.

## Notes

To run the backend: 

```bash
cd backend
uv sync
uv run fastapi dev
```

To run the frontend: 

```bash
cd frontend
npm install
npm run dev
```
